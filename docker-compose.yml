version: "3.8"
services:
  api:
    build: .
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - ACCURACY_MIN=80
      - ALLOW_ORIGIN=*
    volumes:
      - ./:/app
    command: ["node", "server/server.js"]